@setup_workload begin

    # sdof + sdof solvers
    @compile_workload begin

    end

    # noise
    @compile_workload begin

    end

    # Modal solvers
    @compile_workload begin

    end
end